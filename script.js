/* item_navbar_left = document.querySelectorAll(".item");

item_navbar_left.forEach((e) => {
    e.addEventListener('mouseover', () => {
        // Change the button's background color
        e.style.color = 'green';
    });
});

item_navbar_left.forEach((e) => {
    e.addEventListener('mouseout', () => {
        // Change the button's background color
        e.style.color = '';
    });
}); */